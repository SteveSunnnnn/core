name: Core Windows Engine CI

on:
  push:
  pull_request:

jobs:
  release-headless:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4
      - name: Configure
        run: cmake --preset release-headless
      - name: Build
        run: cmake --build --preset release-headless --parallel 2
